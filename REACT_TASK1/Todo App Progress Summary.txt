functionaly requirements - 
1. create and update todo items on user interations. Filter them as per user selection.
2. saving the todo items into DB tables. 
3. fetching the records from tables and show it in UI. create the services to communicate frontend and backend
4. search by title or detail of the todos.
5. write test cases for both UI/UX and API's

Non-functional requirements -
1. create CICD to maintain the sync between environment and publish changes as soon as get uploaded.


DB design -
1. create following tables
    - user_todos (id, title, details, due_date, priority, created_at, updated_at)
    - filter_options(id, filter_type, label, value)